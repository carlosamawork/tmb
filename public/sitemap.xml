<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {/*
  Use JavaScript code to dynamically generate the URLs in the sitemap
  based on the pages in your Next.js project.
  */}
  {pages.map((page) => (
    <url>
      <loc>{`${process.env.NEXT_PUBLIC_SITE_URL}${page}`}</loc>
    </url>
  ))}
</urlset>